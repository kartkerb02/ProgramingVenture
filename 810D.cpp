#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;   
    while(t){
        int n, k;
        cin >> n >> k;
        int a[n], p[n], res[n];
        for(int i  = 0 ; i < n; i++) {
            cin >> a[i]; 
            p[i] = k; 
            res[i] = a[i]/k;N
        }
        int l = a[0], r = a[n-1];


        t--;
    }
    return 0;
}